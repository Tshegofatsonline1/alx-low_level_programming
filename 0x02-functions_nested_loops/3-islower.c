#include "main.h"
/**
 * _islower - Check main
 * @c: input checked
 * Description: program that checks for lowercase character
 *
 * Return: Always 0
 */
int _islower(int c)
{
	char j;

	for (j = 'a'; j <= 'z'; j++)
	{
		if (j == c)
		m=1;
	}

	return (0);
}

